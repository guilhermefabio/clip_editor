# Example plan

`example_plan.json` is a schema illustration, not a validated selection. Replace the source, music hash, verified beat grid and all cuts with your own media. Keep sources long enough for the selected intervals and use a new output directory. Run harness `check` before rendering. No video or music is bundled. `harness/examples/lote2_aprovado.json` remains the historical editorial reference, not a portable dataset.
